export default defineNuxtPlugin(() => {
  // Este plugin garante que as stores sejam registradas automaticamente
  return {
    provide: {
      // Placeholder para futuras funcionalidades globais
    }
  }
})
